import React from 'react';
import styled from 'styled-components';

const Styled = styled.section``;

interface indexProps {}

const index: React.FC<indexProps> = ({}) => {
  return <Styled className=''>Events</Styled>;
};

export default index;
